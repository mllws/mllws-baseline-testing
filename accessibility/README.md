# accessibility/

`pa11y-ci` config (axe or HTML_CodeSniffer runner, WCAG2AA) plus Lighthouse accessibility runs for the scoped URLs. See issue: "Set up accessibility baseline tooling."

No scripts/configs committed yet.
